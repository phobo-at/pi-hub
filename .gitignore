# Secrets / local env
.env
.env.local
*.env.local
# Device-owned kiosk config (KIOSK_BROWSER etc.). Lives only on the Pi today,
# but if a copy is ever made here, `git add -A` would commit it.
.kiosk.env

# Runtime data — populated by the app, must not be committed
data/
!data/.gitkeep

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Claude Code local settings (machine-specific)
.claude/settings.local.json
